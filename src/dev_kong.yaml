# a very minimal declarative config file
_format_version: "2.1"
_transform: true

services:
 - name: chucknorris
   url: https://api.chucknorris.io
   path: /jokes/random
   routes:
     - name: chucknorris-routes
       paths:
         - /chucknorris
       strip_path: true